apiVersion: apps/v1
kind: Deployment
metadata:
  name: driver-service
  labels:
    app: driver-service
    part-of: lojinext-platform
spec:
  replicas: 2
  selector:
    matchLabels: { app: driver-service }
  template:
    metadata:
      labels: { app: driver-service }
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
      containers:
        - name: api
          image: lojinext/driver-service:latest
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: [ALL] }
          ports: [{ containerPort: 8104 }]
          envFrom:
            - configMapRef: { name: driver-service-config }
            - secretRef: { name: driver-service-secrets }
          readinessProbe:
            httpGet: { path: /ready, port: 8104 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits: { cpu: 500m, memory: 512Mi }
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: driver-service-isolation
spec:
  podSelector:
    matchLabels: { app: driver-service }
  policyTypes: [Ingress, Egress]
  ingress:
    - from:
        - podSelector: { matchLabels: { app: nginx-gateway } }
        - podSelector: { matchLabels: { part-of: lojinext-platform } }
      ports: [{ protocol: TCP, port: 8104 }]
  egress:
    - to:
        - podSelector: { matchLabels: { app: redis } }
        - podSelector: { matchLabels: { app: kafka } }
        - podSelector: { matchLabels: { app: postgres } }
    - to:
        [{ ports: [{ protocol: UDP, port: 53 }, { protocol: TCP, port: 53 }] }]
